import { defineConfig } from 'astro/config';

export default defineConfig({
  // Kendi domainini alınca burayı güncelle (ör. https://phalanxracing.com.tr)
  site: 'https://phalanx-racing.github.io',
});
